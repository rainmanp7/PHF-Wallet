cmake_minimum_required(VERSION 2.8.9)
include(CryptoNoteWallet.cmake)

project(${CN_PROJECT_NAME})

execute_process(COMMAND git log -1 --pretty=format:%h
WORKING_DIRECTORY ${CMAKE_SOURCE_DIR}
OUTPUT_VARIABLE GIT_REVISION)

set(CRYPTONOTE_LIB bytecoin)

include_directories(${CMAKE_BINARY_DIR}
  src
  include
  ${CRYPTONOTE_LIB}/external
  ${CRYPTONOTE_LIB}/external/rocksdb/include/
  ${CRYPTONOTE_LIB}/include
  ${CRYPTONOTE_LIB}/src
)

find_package(Qt5Core REQUIRED)
find_package(Qt5Gui REQUIRED)
find_package(Qt5Widgets REQUIRED)
find_package(Qt5Network REQUIRED)
find_package(Qt5DBus REQUIRED)

include_directories(${Qt5DBus_INCLUDE_DIRS} ${CRYPTONOTE_LIB}/external/rocksdb/include/ include src external "${CMAKE_CURRENT_BINARY_DIR}/version")

set(Boost_USE_STATIC_LIBS ON)
if(WIN32)
  set(Boost_USE_STATIC_RUNTIME OFF)
else()
  set(Boost_USE_STATIC_RUNTIME ON)
endif(WIN32)

find_package(Boost 1.58 REQUIRED COMPONENTS date_time filesystem program_options regex serialization system thread chrono)
if ((${Boost_MAJOR_VERSION} EQUAL 1) AND (${Boost_MINOR_VERSION} EQUAL 54))
  message(SEND_ERROR "Boost version <= 1.54 unsupported, more details are available here http://goo.gl/RrCFmA")
endif ()

include_directories(${Boost_INCLUDE_DIRS})
link_directories(${Boost_LIBRARY_DIRS})

set(VERSION "")
configure_file("${CRYPTONOTE_LIB}/src/version.h.in" "version.h")
configure_file("src/CryptoNoteWalletConfig.h.in" "CryptoNoteWalletConfig.h")

add_definitions(-DGIT_REVISION="${GIT_REVISION}")

set(CMAKE_AUTOMOC ON)
set(CMAKE_AUTOUIC ON)
set(CMAKE_AUTORCC ON)
set(CMAKE_INCLUDE_CURRENT_DIR ON)

file(GLOB_RECURSE CRYPTONOTE_SOURCES
  ${CRYPTONOTE_LIB}/external/miniupnpc/connecthostport.c
  ${CRYPTONOTE_LIB}/external/miniupnpc/igd_desc_parse.c
  ${CRYPTONOTE_LIB}/external/miniupnpc/minisoap.c
  ${CRYPTONOTE_LIB}/external/miniupnpc/miniupnpc.c
  ${CRYPTONOTE_LIB}/external/miniupnpc/miniwget.c
  ${CRYPTONOTE_LIB}/external/miniupnpc/minixml.c
  ${CRYPTONOTE_LIB}/external/miniupnpc/portlistingparse.c
  ${CRYPTONOTE_LIB}/external/miniupnpc/receivedata.c
  ${CRYPTONOTE_LIB}/external/miniupnpc/upnpcommands.c
  ${CRYPTONOTE_LIB}/external/miniupnpc/upnpreplyparse.c
  ${CRYPTONOTE_LIB}/src/BlockchainExplorer/BlockchainExplorer.cpp
  ${CRYPTONOTE_LIB}/src/BlockchainExplorer/BlockchainExplorerErrors.cpp
  ${CRYPTONOTE_LIB}/src/Common/Base58.cpp
  ${CRYPTONOTE_LIB}/src/Common/CommandLine.cpp
  ${CRYPTONOTE_LIB}/src/Common/Util.cpp
  ${CRYPTONOTE_LIB}/src/Common/StringTools.cpp
  ${CRYPTONOTE_LIB}/src/Common/JsonValue.cpp
  ${CRYPTONOTE_LIB}/src/Common/ConsoleTools.cpp
  ${CRYPTONOTE_LIB}/src/Common/MemoryInputStream.cpp
  ${CRYPTONOTE_LIB}/src/Common/PathTools.cpp
  ${CRYPTONOTE_LIB}/src/Common/StdInputStream.cpp
  ${CRYPTONOTE_LIB}/src/Common/StdOutputStream.cpp
  ${CRYPTONOTE_LIB}/src/Common/StreamTools.cpp
  ${CRYPTONOTE_LIB}/src/Common/StringOutputStream.cpp
  ${CRYPTONOTE_LIB}/src/Common/StringView.cpp
  ${CRYPTONOTE_LIB}/src/Common/VectorOutputStream.cpp
  ${CRYPTONOTE_LIB}/src/Common/ScopeExit.cpp
  ${CRYPTONOTE_LIB}/src/ConnectivityTool/ConnectivityTool.cpp
  ${CRYPTONOTE_LIB}/src/crypto/blake256.c
  ${CRYPTONOTE_LIB}/src/crypto/chacha8.c
  ${CRYPTONOTE_LIB}/src/crypto/crypto-ops-data.c
  ${CRYPTONOTE_LIB}/src/crypto/crypto-ops.c
  ${CRYPTONOTE_LIB}/src/crypto/crypto.cpp
  ${CRYPTONOTE_LIB}/src/crypto/groestl.c
  ${CRYPTONOTE_LIB}/src/crypto/hash-extra-blake.c
  ${CRYPTONOTE_LIB}/src/crypto/hash-extra-groestl.c
  ${CRYPTONOTE_LIB}/src/crypto/hash-extra-jh.c
  ${CRYPTONOTE_LIB}/src/crypto/hash-extra-skein.c
  ${CRYPTONOTE_LIB}/src/crypto/hash.c
  ${CRYPTONOTE_LIB}/src/crypto/jh.c
  ${CRYPTONOTE_LIB}/src/crypto/keccak.c
  ${CRYPTONOTE_LIB}/src/crypto/oaes_lib.c
  ${CRYPTONOTE_LIB}/src/crypto/random.c
  ${CRYPTONOTE_LIB}/src/crypto/skein.c
  ${CRYPTONOTE_LIB}/src/crypto/slow-hash.c
  ${CRYPTONOTE_LIB}/src/crypto/slow-hash.cpp
  ${CRYPTONOTE_LIB}/src/crypto/tree-hash.c
  ${CRYPTONOTE_LIB}/src/CryptoNoteCore/*.cpp
  ${CRYPTONOTE_LIB}/src/CryptoNoteProtocol/*.cpp
  ${CRYPTONOTE_LIB}/src/Daemon/*.cpp
  ${CRYPTONOTE_LIB}/src/HTTP/*.cpp
  ${CRYPTONOTE_LIB}/src/InProcessNode/InProcessNode.cpp
  ${CRYPTONOTE_LIB}/src/InProcessNode/InProcessNodeErrors.cpp
  ${CRYPTONOTE_LIB}/src/Logging/*.cpp
  ${CRYPTONOTE_LIB}/src/NodeRpcProxy/NodeErrors.cpp
  ${CRYPTONOTE_LIB}/src/NodeRpcProxy/NodeRpcProxy.cpp
  ${CRYPTONOTE_LIB}/src/P2p/*.cpp
  ${CRYPTONOTE_LIB}/src/Rpc/*.cpp
  ${CRYPTONOTE_LIB}/src/Serialization/*.cpp
  ${CRYPTONOTE_LIB}/src/Transfers/BlockchainSynchronizer.cpp
  ${CRYPTONOTE_LIB}/src/Transfers/SynchronizationState.cpp
  ${CRYPTONOTE_LIB}/src/Transfers/TransfersConsumer.cpp
  ${CRYPTONOTE_LIB}/src/Transfers/TransfersContainer.cpp
  ${CRYPTONOTE_LIB}/src/Transfers/TransfersSubscription.cpp
  ${CRYPTONOTE_LIB}/src/Transfers/TransfersSynchronizer.cpp
  ${CRYPTONOTE_LIB}/src/Wallet/*.cpp
  ${CRYPTONOTE_LIB}/src/WalletLegacy/KeysStorage.cpp
  ${CRYPTONOTE_LIB}/src/WalletLegacy/WalletLegacy.cpp
  ${CRYPTONOTE_LIB}/src/WalletLegacy/WalletHelper.cpp
  ${CRYPTONOTE_LIB}/src/WalletLegacy/WalletLegacySerializer.cpp
  ${CRYPTONOTE_LIB}/src/WalletLegacy/WalletLegacySerialization.cpp
  ${CRYPTONOTE_LIB}/src/WalletLegacy/WalletTransactionSender.cpp
  ${CRYPTONOTE_LIB}/src/WalletLegacy/WalletUnconfirmedTransactions.cpp
  ${CRYPTONOTE_LIB}/src/WalletLegacy/WalletUserTransactionsCache.cpp
  ${CRYPTONOTE_LIB}/src/System/ContextGroup.cpp
  ${CRYPTONOTE_LIB}/src/System/Event.cpp
  ${CRYPTONOTE_LIB}/src/System/EventLock.cpp
  ${CRYPTONOTE_LIB}/src/System/InterruptedException.cpp
  ${CRYPTONOTE_LIB}/src/System/Ipv4Address.cpp
  ${CRYPTONOTE_LIB}/src/System/TcpStream.cpp
  ${CRYPTONOTE_LIB}/src/JsonRpcServer/*.cpp
  ${CRYPTONOTE_LIB}/src/PaymentGate/*.cpp
  ${CRYPTONOTE_LIB}/src/PaymentGateService/*.cpp
  ${CRYPTONOTE_LIB}/src/Miner/*.cpp
)

file(GLOB_RECURSE SOURCES src/*.cpp)
file(GLOB_RECURSE HEADERS src/*.h)
file(GLOB_RECURSE UIS src/*.ui)
set(RCC src/resources.qrc)

if(MSVC)
  include_directories(platform/Windows)
  set(SOURCES ${SOURCES} ${CMAKE_SOURCE_DIR}/platform/Windows/ApplicationEventHandler.cpp)
elseif(APPLE)
  include_directories(platform/OSX)
  set(SOURCES ${SOURCES} ${CMAKE_SOURCE_DIR}/platform/OSX/*)
else()
  include_directories(platform/Linux)
  set(SOURCES ${SOURCES} ${CMAKE_SOURCE_DIR}/platform/Linux/ApplicationEventHandler.cpp)
endif()

if (WIN32)
  if (NOT MSVC)
    message(FATAL_ERROR "Only MSVC is supported on this platform")
  endif ()

  add_definitions(/D_CRT_SECURE_NO_WARNINGS /D_WIN32_WINNT=0x0600 /DSTATICLIB)
  include_directories(cryptonote/src/Platform/msc)

  set(PLATFORM_DIR Windows)
  set(BUILD_PLATFORM WIN32)
  set(BUILD_RESOURCES src/bytecoinwallet.rc)
  set(QTMAIN Qt5::WinMain)

elseif (UNIX)

  add_custom_target(
     rocksdb
     COMMAND make static_lib -Wno-undef -Wno-unused-result -Wno-unused-value
     WORKING_DIRECTORY ${CMAKE_CURRENT_SOURCE_DIR}/cryptonote/external/rocksdb
  )
  add_library(rocksdblib STATIC IMPORTED GLOBAL)
  set_target_properties(rocksdblib PROPERTIES IMPORTED_LOCATION ${CMAKE_CURRENT_SOURCE_DIR}/cryptonote/external/rocksdb/librocksdb.a)
  add_dependencies(rocksdblib rocksdb)

  set(CRYPTONOTE_SOURCES ${CRYPTONOTE_SOURCES} cryptonote/external/miniupnpc/minissdpc.c)

  if (APPLE)
    enable_language(ASM)
    file(GLOB_RECURSE OBJC_SOURCES src/*.mm)
    set(SOURCES ${SOURCES} ${OBJC_SOURCES})
    set(PLATFORM_DIR OSX)
    set(MACOSX_BUNDLE_INFO_STRING "Cryptonote GUI wallet")
    set(MACOSX_BUNDLE_LONG_VERSION_STRING "${VERSION_VERSION}.${VERSION_MINOR}.${VERSION_PATCH}")
    set(MACOSX_BUNDLE_BUNDLE_NAME CryptonoteWallet)
    set(MACOSX_BUNDLE_SHORT_VERSION_STRING "${VERSION_VERSION}.${VERSION_MINOR}.${VERSION_PATCH}")
    set(MACOSX_BUNDLE_BUNDLE_VERSION "$${VERSION_VERSION}.${VERSION_MINOR}.${VERSION_PATCH}")

    find_package(Qt5PrintSupport REQUIRED)

    include_directories(/usr/include/malloc)

    set(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -maes -std=c++11 -stdlib=libc++")
    set(CMAKE_C_FLAGS "${CMAKE_C_FLAGS} -std=c11 -maes -D_DARWIN_C_SOURCE")
    set(CMAKE_EXE_LINKER_FLAGS "${CMAKE_EXE_LINKER_FLAGS} -framework Cocoa -framework OpenGL -framework CoreFoundation -framework Carbon -framework IOKit -L/usr/lib")

    set(MACOSX_BUNDLE_ICON_FILE cryptonote.icns)
    set(APPLICATION_ICON src/images/cryptonote.icns)
    set_source_files_properties(${APPLICATION_ICON} PROPERTIES MACOSX_PACKAGE_LOCATION "Resources")

    set(BUILD_PLATFORM MACOSX_BUNDLE)
    set(BUILD_RESOURCES ${APPLICATION_ICON})

    GET_TARGET_PROPERTY(QT_LIB_DIR "${Qt5Widgets_LIBRARIES}" LOCATION)
    GET_FILENAME_COMPONENT(QT_LIB_DIR "${QT_LIB_DIR}" PATH)

  else(APPLE)

    set(PLATFORM_DIR Linux)
    set(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -maes -std=c++11")
    set(CMAKE_C_FLAGS "${CMAKE_C_FLAGS} -maes -std=c11")
  endif (APPLE)
endif ()

include_directories(${CRYPTONOTE_LIB}/src/Platform/${PLATFORM_DIR})
file(GLOB PLATFORM_SOURCES ${CRYPTONOTE_LIB}/src/Platform/${PLATFORM_DIR}/System/*)
set(CRYPTONOTE_SOURCES ${CRYPTONOTE_SOURCES} ${PLATFORM_SOURCES} ${CRYPTONOTE_PLATFORM})

add_library(${CRYPTONOTE_LIB} STATIC ${CRYPTONOTE_SOURCES})
set_target_properties(${CRYPTONOTE_LIB} PROPERTIES COMPILE_DEFINITIONS _GNU_SOURCE)
target_link_libraries(${CRYPTONOTE_LIB} ${Boost_LIBRARIES} ${Qt5DBus_LIBRARIES} rocksdblib)

add_executable(${PROJECT_NAME} ${BUILD_PLATFORM} ${BUILD_RESOURCES} ${SOURCES} ${HEADERS} ${UIS} ${RCC})
set_target_properties(${PROJECT_NAME} PROPERTIES COMPILE_DEFINITIONS _GNU_SOURCE)
target_link_libraries(${PROJECT_NAME} ${Boost_LIBRARIES} ${QTMAIN} ${CRYPTONOTE_LIB} ${Qt5DBus_LIBRARIES} rocksdblib Qt5::Widgets Qt5::Gui Qt5::Network)

if (APPLE)
  target_link_libraries(${PROJECT_NAME} PrintSupport)
elseif (UNIX)
  target_link_libraries(${PROJECT_NAME} -lpthread)
elseif (WIN32)
  target_link_libraries(${PROJECT_NAME} Imm32 Iphlpapi Rpcrt4 Winmm Wtsapi32)
endif (APPLE)
